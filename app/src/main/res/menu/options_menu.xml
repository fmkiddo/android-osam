<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/opt_search"
        android:icon="@drawable/ic_search"
        android:title="@string/opt_search"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/opt_setting"
        android:title="@string/opt_setting"
        app:showAsAction="never" />

    <item
        android:id="@+id/opt_logout"
        android:title="@string/opt_logout"
        app:showAsAction="never" />

    <item
        android:id="@+id/opt_exit"
        android:title="@string/opt_exit"
        app:showAsAction="never" />
</menu>